import React from "react";
import { ThemedText } from "@/components/themed-text";
import { ThemedView } from "@/components/themed-view";

export default function SellerrHome() {

    return(
        <ThemedView>
            <ThemedText>Seller Home Screen</ThemedText>
        </ThemedView>
    )
}